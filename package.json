{
  "name": "meting-agent-repo",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:mcp": "node scripts/sync-mcp-core.mjs",
    "sync:skill": "node scripts/build-skill-release.mjs",
    "sync:all": "npm run sync:mcp && npm run sync:skill",
    "build:skill": "npm run sync:skill",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
