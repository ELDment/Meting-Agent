interface:
  display_name: "Meting Agent"
  short_description: "Use bundled Meting scripts for direct music lookup tasks."
  default_prompt: "Use $meting-agent to query music data through the bundled scripts in this skill."
